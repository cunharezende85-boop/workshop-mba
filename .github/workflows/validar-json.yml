name: Validar workshops.json

on:
  push:
    paths:
      - 'workshops.json'

jobs:
  validar:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verificar se o JSON é válido
        run: python3 -m json.tool workshops.json > /dev/null
